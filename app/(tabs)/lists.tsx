import { Text } from 'react-native';
import App from '../../App';
import { ScreenView } from '../../components';

export default function Screen() {
  return (
    <ScreenView>
      <Text>Lists</Text>
    </ScreenView>
  );
}
